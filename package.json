{
  "name": "stripe-terminal-backend",
  "version": "1.0.0",
  "description": "Simple Stripe Terminal backend for Vercel deployment",
  "main": "api/connection-token.js",
  "scripts": {
    "dev": "vercel dev"
  },
  "dependencies": {
    "stripe": "^12.0.0"
  },
  "devDependencies": {
    "vercel": "^28.0.0"
  }
}
